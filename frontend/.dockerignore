# 의존성 / 빌드 산출물 (이미지 안에서 새로 설치·빌드함)
node_modules
.next
out
coverage
*.tsbuildinfo

# 환경변수 (절대 이미지에 포함하지 않음)
.env
.env.*
!.env.example

# 버전관리/에디터/OS
.git
.gitignore
.vscode
.idea
.DS_Store

# 문서/인프라 파일 (빌드 컨텍스트에 불필요)
*.md
Dockerfile
.dockerignore
docker-compose*.yml
